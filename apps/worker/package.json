{
  "name": "worker",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "A worker, for Flowlink, will be used to send solana and mails to appropriate destination.",
  "dependencies": {
    "@flowlink/kafka": "workspace:*",
    "@flowlink/db": "workspace:*"
  },
  "devDependencies": {
    "@flowlink/typescript-config": "workspace:*"
  }
}
